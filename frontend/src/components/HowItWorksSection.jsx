import React from "react";import { motion } from "framer-motion";import StepCard from "./StepCard";const HowItWorksSection = () => {    const steps = [        {            number: "1",            title: "Select Files",            description: "Choose files from your device to share securely",            bgColor: "from-blue-500 to-blue-600",            accentColor: "bg-blue-200",        },        {            number: "2",            title: "Get Code",            description: "Generate a unique session code instantly",            bgColor: "from-green-500 to-green-600",            accentColor: "bg-green-200",        },        {            number: "3",            title: "Share Code",            description: "Receiver enters the code to establish connection",            bgColor: "from-purple-500 to-purple-600",            accentColor: "bg-purple-200",        },        {            number: "4",            title: "Transfer",            description: "Files transfer directly between devices safely",            bgColor: "from-orange-500 to-orange-600",            accentColor: "bg-orange-200",        },    ];    return (        <motion.div            initial={{ opacity: 0, y: 30 }}            animate={{ opacity: 1, y: 0 }}            transition={{ duration: 0.8, delay: 0.4 }}            className="bg-white/80 backdrop-blur-sm p-12 rounded-3xl shadow-xl border border-white/50 mb-16"        >            <div className="text-center mb-12">                <h2 className="text-3xl sm:text-4xl font-bold text-gray-800 mb-2">                    How it works                </h2>                <div className="w-30 h-1 mx-auto rounded-full title-gradient-bar"></div>            </div>            <div className="grid md:grid-cols-4 gap-8">                {steps.map((step, index) => (                    <StepCard                        key={index}                        number={step.number}                        title={step.title}                        description={step.description}                        bgColor={step.bgColor}                        accentColor={step.accentColor}                    />                ))}            </div>        </motion.div>    );};export default HowItWorksSection;